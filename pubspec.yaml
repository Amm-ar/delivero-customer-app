name: customer_app
description: Delivero - Food Delivery Customer App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  provider: ^6.1.5+1
  
  # HTTP & API
  dio: ^5.4.0
  http: ^1.1.2
  
  # Local Storage
  shared_preferences: ^2.5.4
  flutter_secure_storage: ^9.0.0
  
  # Maps & Location (OpenStreetMap - Free!)
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  geolocator: ^13.0.2
  geocoding: ^3.0.0
  
  # Real-time Communication
  socket_io_client: ^2.0.3+1
  
  # Payments
  flutter_stripe: ^10.1.0
  
  # Push Notifications  
  firebase_core: ^2.24.2
  firebase_messaging: ^14.7.6
  flutter_local_notifications: ^17.0.0
  
  # UI Components
  cached_network_image: ^3.3.0
  fl_chart: ^0.65.0
  shimmer: ^3.0.0
  lottie: ^2.7.0
  flutter_svg: ^2.0.9
  
  # Icons & Fonts
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  
  # Image Picker
  image_picker: ^1.0.5
  
  # Utilities
  url_launcher: ^6.2.2
  timeago: ^3.6.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  generate: true
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/
    - assets/animations/
    

